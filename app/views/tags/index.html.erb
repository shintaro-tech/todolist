<div class= "mb-4">
  <h1>
    <span class="mark">カテゴリ一覧</span>
  </h1>
</div>

 <table class="table table-striped">
    <tbody>
      <% @tags.each do |tag| %>
        <tr>
          <td><%= link_to tag.title, tag %></td>
        </tr>
      <% end %>
    </tbody>
 </table>

 


<%= link_to '新規カテゴリの追加', new_tag_path, class: "btn btn-primary" %>
